<UserControl x:Class="ClaudeSessionBackup.App.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:ClaudeSessionBackup.App.Converters">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Themes/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Settings" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12" />

        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <!-- Destination -->
                <TextBlock Text="Backup destination" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,8" />
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0"
                                Text="{Binding Destination, UpdateSourceTrigger=PropertyChanged}"
                                Margin="0,0,8,0" />
                    <ui:Button Grid.Column="1" Content="Browse..."
                               Command="{Binding BrowseDestinationCommand}" />
                </Grid>

                <!-- Include subagents -->
                <CheckBox Content="Include subagent transcripts (~1.2 GB of workflow scratch)"
                          IsChecked="{Binding IncludeSubagents}"
                          Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                          Margin="0,0,0,12" />

                <!-- Keep snapshots -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Keep snapshots (0 = all):"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    <ui:TextBox Grid.Column="1"
                                Text="{Binding KeepSnapshots, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <!-- Dark/light theme -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Theme:"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               Margin="0,0,12,0" />
                    <ui:Button Content="Toggle dark / light"
                               Command="{Binding ToggleThemeCommand}" />
                </StackPanel>

                <!-- Save -->
                <ui:Button Content="Save settings" Appearance="Primary"
                           Command="{Binding SaveSettingsCommand}" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
